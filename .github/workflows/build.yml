name: Debug Workflow

on:
  push:
    branches:
      - main

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Open shell
        run: |
          tmux new-session -d -s debug
          tmux attach-session -t debug
